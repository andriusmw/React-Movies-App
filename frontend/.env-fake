VITE_API_KEY=YOUR_APIKEY
VITE_BASE_URL=https://api.themoviedb.org/3

/*---------------------------------------

Just change those values without "" or ; or any
signs, all toguether no spaces after the = sign.
And rename it to ".env" then make sure it connects
and all variables are called correctly in "api.js".
We also should not call an API on the front, allways use 
a backend so all variables are not visible on the web browser
or encrypted.

----------------------------------------*/